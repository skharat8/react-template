import LogoutButton from "@/features/authentication/LogoutButton";

function Header() {
  return (
    <header>
      <p>Twitter</p>
      <LogoutButton />
    </header>
  );
}

export default Header;
